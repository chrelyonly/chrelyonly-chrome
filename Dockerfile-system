# 使用最小化的 OpenJDK 运行时环境
FROM selenium/standalone-chrome

#设置工作目录
#WORKDIR /app

# 将应用的 JAR 文件添加到容器中
#ADD target/chrelyonly-chrome-0.0.1-SNAPSHOT.jar /app/app.jar
#ADD chromedriver /app/chrome/chromedriver
#ADD chrome-linux64 /app/chrome/
#给予权限
# 定义容器启动时运行的命令
#CMD ["java", "-jar", "app.jar"]
#CMD ["sh"]
